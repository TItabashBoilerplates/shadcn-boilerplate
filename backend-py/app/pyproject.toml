
[tool.mypy]
python_version = "3.13"
mypy_path = "src"
explicit_package_bases = true
exclude = [
    '^\.venv/',
]
strict = true
ignore_missing_imports = true
install_types = true
non_interactive = true

[tool.ruff]
# Same as Black.
line-length = 88

# Assume Python 3.11.
target-version = "py312"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "ANN1",   # missing type self and cls
    "D1",     # undocumented
    "TD001",  # invalid todo tag
    "TD002",  # missing todo author
    "TD003",  # missing todo link
    "PD011",  # pandas use of dot values
    "B008",
    "COM812", # conflicts with formatter
    "TC",     # flake8-type-checking: pydantic/SQLModel evaluate annotations at runtime
]
unfixable = [
    "F401",   # unused import
    "F841",   # unused variable
    "ERA001", # commented out code
]

logger-objects = ["src.library.logger.LOGGER"]

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = ["controller", "usecase", "gateway", "domain", "infra", "middleware"]
section-order = ["future", "standard-library", "third-party", "first-party", "local-folder"]
split-on-trailing-comma = true

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
# Like Black, use double quotes for strings.
quote-style = "double"
docstring-code-format = true

[lint.mccabe]
max-complexity = 3

[project]
name = "backend-py"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "orjson ==3.10.14",
    "fastapi",
    "starlette",
    "uvicorn[standard]",
    # "python-multipart",  # Form()/File()/UploadFile 未使用
    "supabase",
    # "strawberry-graphql",  # GraphQL 未使用
    # "pandas",  # データ処理 未使用
    # "numpy",  # 数値計算 未使用
    # "scipy",  # 科学計算 未使用
    # "httpx",  # 直接import無し（supabaseの依存で含まれる）

    # SQLModel & Database
    "sqlmodel",
    "psycopg2-binary",

    # Autogen
    # "pyautogen",  # マルチエージェント 未使用

    # OpenAI
    # "openai",  # langchain-openai 経由で使用するため直接依存不要

    # LangChain
    # "langchain",  # langchain_core のみ使用（langchain-openai の依存で含まれる）
    # "langchain-community",  # 未使用
    # "langchain-anthropic",  # 未使用
    "langchain-openai",
    # "langchain-postgres",  # ベクトルストア 未使用
    # "langgraph",  # エージェント状態管理 未使用
    # "langchainhub",  # プロンプトテンプレート 未使用

    # 画像
    # "Pillow",  # 画像処理 未使用

    # 画像生成AI Client
    # "replicate",  # 未使用
    # "fal-client",  # 未使用

    # runpod
    # "runpod",  # GPU推論 未使用

    # Modal
    # "modal",  # コンテナ化推論 未使用

    # AI
    # "torch",  # 深層学習 未使用

    # WebRTC
    # "aiortc",  # 未使用
    # "livekit",  # 未使用
    # "livekit-api",  # 未使用

    # Message Queue
    # "kombu",  # 未使用
    # "tembo-pgmq-python",  # 未使用

    # AWS
    # "boto3",

    # Google Cloud
    # "google-cloud-bigquery-data-exchange",

    # Azure
    # "azure-identity",

    # Snowflake
    # "snowflake-connector-python",

    # 音声
    # "cartesia",  # 未使用

    # Logging
    "structlog",

    # "pyyaml>=6.0.2",  # 直接import無し
    # "oyaml>=1.0",  # 直接import無し
]

[dependency-groups]
dev = [
    # Linting & Formatting & Type Checking
    "ruff",
    "mypy",

    # Testing
    "pytest",
    "pytest-cov",
    "pytest-asyncio",
    "pytest-mock",
    "testcontainers[postgres]",
    "httpx",

    # 画像生成AI
    # "diffusers",  # 未使用
    # "transformers",  # 未使用
    # "sentencepiece",  # 未使用
    # "protobuf",  # 未使用
    # "accelerate",  # 未使用
    # "compel",  # 未使用
    # "torchao",  # 未使用
    # "huggingface-hub",  # 未使用
]

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
pythonpath = ["src"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-ra -q --cov=src --cov-report=term-missing --cov-report=html"
asyncio_mode = "auto"

# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"
