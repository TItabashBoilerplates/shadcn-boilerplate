name: DB Migrate (Drizzle)

# PaaS ネイティブ Git 連携が config.toml / Edge Functions / Storage buckets を担うのに対し、
# **Drizzle マイグレーションだけ**はどの PaaS でも流せない（native は supabase/migrations/*.sql
# しか読まず、Drizzle は drizzle/migrations/ のフォルダ形式 = 対象外）。公式も「自前 ORM の
# migration は GitHub Actions で流せ」と明記。よってこの最小ワークフローが唯一の Actions
# デプロイ部品。設計の背景は docs/deployment/README.md / .claude/rules/database.md を参照。
#
# branch→env: main=production / staging=staging / develop=dev
# 本番(production)のみ GitHub Environment の required reviewers で手動承認ゲート（database.md 準拠）。
# dev/staging は自動適用。

on:
  push:
    branches: [main, staging, develop]
    # スキーマ/マイグレに関係する変更時のみ起動（無駄な run を避ける）
    paths:
      - "drizzle/schema/**"
      - "drizzle/migrations/**"
      - "drizzle/config/**"
      # migration ランナー自体の変更も反映対象（これが無いと runner を直しても流れない）
      - "drizzle/migrate.ts"
      - "drizzle/drizzle.config.ts"
      - "drizzle/package.json"
      - "drizzle/bun.lock"
      - ".github/workflows/migrate.yml"
  # 手動実行: Actions タブ > "DB Migrate (Drizzle)" > Run workflow で
  # branch と対象環境を選ぶ。production は GitHub Environment の
  # deployment branch policy(main) + required reviewers に従う。
  workflow_dispatch:
    inputs:
      environment:
        description: "対象環境（手動実行時）"
        type: choice
        options: [dev, staging, production]
        default: dev

concurrency:
  group: migrate-${{ github.ref }}
  cancel-in-progress: false

jobs:
  migrate:
    name: db:migrate-deploy
    runs-on: ubuntu-latest
    # production を選んだ run のみ承認ゲートがかかる（dev/staging は reviewers 無し = 自動）。
    # env スコープの secret（Doppler が sync 済み）もここで選択した environment から解決される。
    environment: >-
      ${{ github.event_name == 'workflow_dispatch' && inputs.environment
        || (github.ref == 'refs/heads/main' && 'production')
        || (github.ref == 'refs/heads/staging' && 'staging')
        || 'dev' }}
    env:
      # 接続先 DB。**Doppler → GitHub のネイティブ sync** が、Doppler の各 config を対応する
      # GitHub Environment の secrets に配っている（prd→production / stg→staging / dev→dev）。
      # 上の environment: で選ばれた env スコープの secret がここで解決される。
      # → Actions 内で doppler CLI を叩く必要は無い（service token も不要）。
      #
      # ⚠️ 変数名が `POSTGRES_URL` **ではない**のは意図的。devenv の enterShell は
      # `set -a; . env/<svc>/.env.$ENV` を行うため、その env ファイルが定義する変数は
      # **外から渡した同名の値を上書きする**（実測: ENV 未指定だと 127.0.0.1:54322 に化ける）。
      # `MIGRATE_POSTGRES_URL` は devenv も env ファイルも定義しない名前なので確実に伝わり、
      # db:migrate-deploy task が最後に POSTGRES_URL へ反映する。
      MIGRATE_POSTGRES_URL: ${{ secrets.POSTGRES_URL }}
      # 値は GitHub Secrets から来るので、devenv の Doppler ロードは行わない（doppler CLI 不使用）。
      DOPPLER_SKIP: "1"
      # ENV は Doppler config / env ファイルの選択に使われる。上記の輸送用変数があるので接続先の
      # 正しさは ENV に依存しないが、profile を明示するために渡す（実測で伝播することを確認済み）。
      ENV: >-
        ${{ github.event_name == 'workflow_dispatch' && inputs.environment
          || (github.ref == 'refs/heads/main' && 'production')
          || (github.ref == 'refs/heads/staging' && 'staging')
          || 'dev' }}
    defaults:
      run:
        shell: devenv shell bash -- -e {0}
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            keep-outputs = true
            keep-env-derivations = true
      - uses: nix-community/cache-nix-action@v7
        with:
          primary-key: nix-${{ runner.os }}-${{ hashFiles('devenv.nix', 'devenv.lock', 'devenv.yaml') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-
          gc-max-store-size-linux: 5G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-
          purge-created: 0
          purge-primary-key: never
      - uses: cachix/cachix-action@v16
        with:
          name: devenv
      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            drizzle/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('drizzle/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-
      - name: Install devenv.sh
        shell: bash
        run: nix profile add nixpkgs#devenv
      # 接続先が GitHub Secrets（Doppler sync 済み）から解決できたことを、適用前に検証する。
      # devenv shell を経由する間に env ファイル等で上書きされていないかもここで検出する
      # （.claude/rules/error-handling.md: 握りつぶさない）。
      # ⚠️ POSTGRES_URL は DB パスワードを含むので **絶対に出力しない**。host:port だけ表示する。
      - name: Verify remote credentials resolved
        run: |
          if [ -z "${MIGRATE_POSTGRES_URL:-}" ]; then
            echo "::error::MIGRATE_POSTGRES_URL が空です。GitHub Environment '${ENV}' に POSTGRES_URL secret が同期されているか確認してください（Doppler > Integrations > GitHub で該当 config → 該当 Environment の sync を作成）。"
            exit 1
          fi
          case "$MIGRATE_POSTGRES_URL" in
            *127.0.0.1*|*localhost*)
              echo "::error::接続先がローカル値です。GitHub Environment '${ENV}' に同期されている POSTGRES_URL の値を確認してください。"
              exit 1 ;;
          esac
          # 認証情報を落として host:port のみ表示
          echo "✓ ENV=${ENV} / target=$(printf '%s' "$MIGRATE_POSTGRES_URL" | sed -E 's|^[^@]*@||; s|[/?].*$||')"
      # raw drizzle-kit ではなく devenv task を使う（.claude/rules/commands.md 準拠）。
      # ENV は job env で渡し済みなので -P は profile を明示するための冗長指定。
      - name: Run Drizzle migrations
        run: devenv tasks run -P "$ENV" db:migrate-deploy
      # 手動実行（とくに production）の監査痕跡。run 一覧から「誰がどの env に流したか」が見える。
      - name: Job summary
        if: always()
        shell: bash
        run: |
          {
            echo "### DB Migrate (Drizzle)"
            echo ""
            echo "| 項目 | 値 |"
            echo "|---|---|"
            echo "| profile / env | \`${ENV}\` |"
            echo "| trigger | \`${{ github.event_name }}\` |"
            echo "| ref | \`${{ github.ref_name }}\` (\`${{ github.sha }}\`) |"
            echo "| actor | @${{ github.actor }} |"
            echo "| result | \`${{ job.status }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"
