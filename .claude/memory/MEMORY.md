# Memory Index

## Feedback
- [devenv コマンド必須](feedback_use_make_commands.md) - 品質チェックは必ず devenv のコマンド（scripts / `devenv tasks run`）を使用。Makefile は deprecated。

## Project
- [devenv up の既定起動セット](project_devenv_up_default.md) - `devenv up` は軽量セット (Supabase + backend + storybook) のみ。frontend monorepo apps は opt-in。
