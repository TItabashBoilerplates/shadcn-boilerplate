{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "メインウィンドウに与える既定の権限。必要になったものだけをここに足すこと（Tauri v2 は v1 の allowlist と違い、ウィンドウ単位で権限を絞る）。updater は自動更新の確認・インストール、process は更新後の再起動（relaunch）に要る。",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default", "process:default"]
}
